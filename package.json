{
  "name": "assignment-ui",
  "description": "Project for UI test automation assignment",
  "scripts": {
    "setup": "webdriver-manager update",
    "test": "protractor test/protractor/conf.js"
  },
  "devDependencies": {
    "jasmine-spec-reporter": "^7.0.0",
    "protractor": "^7.0.0",
    "webdriver-manager": "^12.1.8"
  }
}
